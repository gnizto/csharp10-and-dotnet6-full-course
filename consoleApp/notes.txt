> To add summary use the following command before the method

`c#
\\\ <summary>
\\\ Summary goes here
\\\ <\summary>
`

> A shortcut for `Console.WriteLine` is `cw`

> We can only reference static variables from static methods

> In C#, every executed instruction is performed in the context of mthod. 
  The Main method is the entry point for every C# application.

> Meethod Syntax
  <Access Specifier> <Return Type> <Method Name>(Parameter List)
  {
	Method Body
  }

> Object might be called "instance"

> A shortcut for creating a new property is `prop`

> Remember to use "Immediate Window" -> Debug > Windows > Immediate (or just Ctrl + Alt + I)